server :
  port: 8002
  
spring :
  application :
    name: showtime-engine
    
  datasource:
        url: jdbc:mysql://localhost:3306/showtime_db?createDatabaseIfNotExist=true
        username: root
        password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8767/eureka
